# Required only to create the agent (scripts/create_agent.py)
ELEVENLABS_API_KEY=

# Optional. When set, /qualify rejects requests without a valid X-Signature header.
ELEVENLABS_WEBHOOK_SECRET=

# Optional. When unset, the CRM step runs in dry-run and prints what it would send.
HUBSPOT_TOKEN=
